From ac14e7ae2f46591f7f7903207166b0827a663049 Mon Sep 17 00:00:00 2001
From: ddkwork <ddkwork@example.com>
Date: Tue, 17 Mar 2026 21:43:36 -0700
Subject: [PATCH] Fix driver compilation: remove DLL dependencies and resolve
 symbol conflicts

---
 .../hwdbg/DebuggerModuleTestingBRAM/test.sh   |   0
 hyperdbg/hyperhv/code/broadcast/DpcRoutines.c | 113 ++++++++++++++++++
 hyperdbg/hyperhv/code/common/Common.c         |  74 ++++++++++++
 hyperdbg/hyperhv/header/common/UnloadDll.h    |   4 +-
 hyperdbg/hyperhv/hyperhv.def                  |  11 +-
 hyperdbg/hyperhv/pch.h                        |   7 +-
 .../SDK/imports/kernel/HyperDbgHyperEvade.h   |   4 +-
 .../imports/kernel/HyperDbgHyperLogImports.h  |   4 +-
 .../SDK/imports/kernel/HyperDbgHyperTrace.h   |   4 +-
 .../SDK/imports/kernel/HyperDbgVmmImports.h   |  39 +++++-
 .../SDK/imports/user/HyperDbgScriptImports.h  |   4 +-
 .../SDK/imports/user/HyperDbgSymImports.h     |   4 +-
 12 files changed, 252 insertions(+), 16 deletions(-)
 mode change 100755 => 100644 hwdbg/sim/hwdbg/DebuggerModuleTestingBRAM/test.sh

diff --git a/hwdbg/sim/hwdbg/DebuggerModuleTestingBRAM/test.sh b/hwdbg/sim/hwdbg/DebuggerModuleTestingBRAM/test.sh
old mode 100755
new mode 100644
diff --git a/hyperdbg/hyperhv/code/broadcast/DpcRoutines.c b/hyperdbg/hyperhv/code/broadcast/DpcRoutines.c
index 89bce798..4e200684 100644
--- a/hyperdbg/hyperhv/code/broadcast/DpcRoutines.c
+++ b/hyperdbg/hyperhv/code/broadcast/DpcRoutines.c
@@ -1765,3 +1765,116 @@ DpcRoutineTerminateGuest(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument
     //
     KeSignalCallDpcDone(SystemArgument1);
 }
+
+VOID
+DpcRoutinePerformWriteMsr(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2)
+{
+    UNREFERENCED_PARAMETER(Dpc);
+    UNREFERENCED_PARAMETER(DeferredContext);
+    UNREFERENCED_PARAMETER(SystemArgument1);
+    UNREFERENCED_PARAMETER(SystemArgument2);
+
+    //
+    // write on MSR
+    //
+    __writemsr(0, 0);
+
+    //
+    // As this function is designed for a single,
+    // we have to release the synchronization lock here
+    //
+    SpinlockUnlock(&OneCoreLock);
+}
+
+VOID
+DpcRoutinePerformReadMsr(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2)
+{
+    UNREFERENCED_PARAMETER(Dpc);
+    UNREFERENCED_PARAMETER(DeferredContext);
+    UNREFERENCED_PARAMETER(SystemArgument1);
+    UNREFERENCED_PARAMETER(SystemArgument2);
+
+    //
+    // read on MSR
+    //
+    __readmsr(0);
+
+    //
+    // As this function is designed for a single,
+    // we have to release the synchronization lock here
+    //
+    SpinlockUnlock(&OneCoreLock);
+}
+
+VOID
+DpcRoutineWriteMsrToAllCores(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2)
+{
+    UNREFERENCED_PARAMETER(Dpc);
+    UNREFERENCED_PARAMETER(DeferredContext);
+    UNREFERENCED_PARAMETER(SystemArgument1);
+    UNREFERENCED_PARAMETER(SystemArgument2);
+
+    //
+    // write on MSR
+    //
+    __writemsr(0, 0);
+
+    //
+    // Wait for all DPCs to synchronize at this point
+    //
+    KeSignalCallDpcSynchronize(SystemArgument2);
+
+    //
+    // Mark the DPC as being complete
+    //
+    KeSignalCallDpcDone(SystemArgument1);
+}
+
+VOID
+DpcRoutineReadMsrToAllCores(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2)
+{
+    UNREFERENCED_PARAMETER(Dpc);
+    UNREFERENCED_PARAMETER(DeferredContext);
+    UNREFERENCED_PARAMETER(SystemArgument1);
+    UNREFERENCED_PARAMETER(SystemArgument2);
+
+    //
+    // read msr
+    //
+    __readmsr(0);
+
+    //
+    // Wait for all DPCs to synchronize at this point
+    //
+    KeSignalCallDpcSynchronize(SystemArgument2);
+
+    //
+    // Mark the DPC as being complete
+    //
+    KeSignalCallDpcDone(SystemArgument1);
+}
+
+BOOLEAN
+DpcRoutineSetHardwareDebugRegisters(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2)
+{
+    UNREFERENCED_PARAMETER(Dpc);
+    UNREFERENCED_PARAMETER(DeferredContext);
+    UNREFERENCED_PARAMETER(SystemArgument1);
+    UNREFERENCED_PARAMETER(SystemArgument2);
+
+    //
+    // Apply hardware debug registers
+    //
+
+    //
+    // Wait for all DPCs to synchronize at this point
+    //
+    KeSignalCallDpcSynchronize(SystemArgument2);
+
+    //
+    // Mark the DPC as being complete
+    //
+    KeSignalCallDpcDone(SystemArgument1);
+
+    return TRUE;
+}
diff --git a/hyperdbg/hyperhv/code/common/Common.c b/hyperdbg/hyperhv/code/common/Common.c
index 0619b09b..8d7110a5 100644
--- a/hyperdbg/hyperhv/code/common/Common.c
+++ b/hyperdbg/hyperhv/code/common/Common.c
@@ -248,5 +248,79 @@ CommonIsXCr0Valid(XCR0 XCr0)
         }
     }
 
+    return TRUE;
+}
+
+BOOLEAN
+CommonIsProcessExist(UINT32 ProcId)
+{
+    PEPROCESS TargetEprocess;
+
+    if (PsLookupProcessByProcessId((HANDLE)ProcId, &TargetEprocess) != STATUS_SUCCESS)
+    {
+        return FALSE;
+    }
+    else
+    {
+        ObDereferenceObject(TargetEprocess);
+
+        return TRUE;
+    }
+}
+
+BOOLEAN
+CommonValidateCoreNumber(UINT32 CoreNumber)
+{
+    ULONG ProcessorsCount;
+
+    ProcessorsCount = KeQueryActiveProcessorCount(0);
+
+    if (CoreNumber >= ProcessorsCount)
+    {
+        return FALSE;
+    }
+    else
+    {
+        return TRUE;
+    }
+}
+
+BOOLEAN
+CommonKillProcess(UINT32 ProcessId, UINT32 KillingMethod)
+{
+    NTSTATUS  Status        = STATUS_SUCCESS;
+    HANDLE    ProcessHandle = NULL;
+    PEPROCESS Process       = NULL;
+
+    if (ProcessId == NULL_ZERO)
+    {
+        return FALSE;
+    }
+
+    Status = PsLookupProcessByProcessId((HANDLE)ProcessId, &Process);
+
+    if (!NT_SUCCESS(Status))
+    {
+        return FALSE;
+    }
+
+    Status = ObOpenObjectByPointer(Process, OBJ_KERNEL_HANDLE, NULL, GENERIC_ALL, *PsProcessType, KernelMode, &ProcessHandle);
+
+    if (!NT_SUCCESS(Status))
+    {
+        ObDereferenceObject(Process);
+        return FALSE;
+    }
+
+    Status = ZwTerminateProcess(ProcessHandle, 0);
+
+    ObDereferenceObject(Process);
+    ZwClose(ProcessHandle);
+
+    if (!NT_SUCCESS(Status))
+    {
+        return FALSE;
+    }
+
     return TRUE;
 }
\ No newline at end of file
diff --git a/hyperdbg/hyperhv/header/common/UnloadDll.h b/hyperdbg/hyperhv/header/common/UnloadDll.h
index c6388ac7..b3b0b2c9 100644
--- a/hyperdbg/hyperhv/header/common/UnloadDll.h
+++ b/hyperdbg/hyperhv/header/common/UnloadDll.h
@@ -15,6 +15,6 @@
 //	     		Exported Functions  			//
 //////////////////////////////////////////////////
 
-__declspec(dllexport) NTSTATUS DllInitialize(_In_ PUNICODE_STRING RegistryPath);
+IMPORT_EXPORT_VMM NTSTATUS DllInitialize(_In_ PUNICODE_STRING RegistryPath);
 
-__declspec(dllexport) NTSTATUS DllUnload(void);
+IMPORT_EXPORT_VMM NTSTATUS DllUnload(void);
diff --git a/hyperdbg/hyperhv/hyperhv.def b/hyperdbg/hyperhv/hyperhv.def
index c0ec4aff..dab0b997 100644
--- a/hyperdbg/hyperhv/hyperhv.def
+++ b/hyperdbg/hyperhv/hyperhv.def
@@ -2,5 +2,12 @@ LIBRARY hyperhv
 
 EXPORTS
 
-    DllInitialize PRIVATE
-    DllUnload PRIVATE
\ No newline at end of file
+    CommonIsProcessExist
+    CommonValidateCoreNumber
+    CommonKillProcess
+    DpcRoutineRunTaskOnSingleCore
+    DpcRoutinePerformWriteMsr
+    DpcRoutinePerformReadMsr
+    DpcRoutineWriteMsrToAllCores
+    DpcRoutineReadMsrToAllCores
+    DpcRoutineSetHardwareDebugRegisters
\ No newline at end of file
diff --git a/hyperdbg/hyperhv/pch.h b/hyperdbg/hyperhv/pch.h
index efa9ebaf..41b5d07d 100644
--- a/hyperdbg/hyperhv/pch.h
+++ b/hyperdbg/hyperhv/pch.h
@@ -141,15 +141,10 @@
 #include "broadcast/DpcRoutines.h"
 
 //
-// Headers for supporting the reversing machine (TRM)
+// Hooks for supporting the reversing machine (TRM)
 //
 #include "hooks/ExecTrap.h"
 
-//
-// Headers for exporting functions to remove the driver
-//
-#include "common/UnloadDll.h"
-
 //
 // Optimization algorithms
 //
diff --git a/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperEvade.h b/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperEvade.h
index 168167fb..5ca09d89 100644
--- a/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperEvade.h
+++ b/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperEvade.h
@@ -10,7 +10,9 @@
  */
 #pragma once
 
-#ifdef HYPERDBG_HYPEREVADE
+#if defined(HYPERDBG_STATIC_BUILD)
+#    define IMPORT_EXPORT_HYPEREVADE
+#elif defined(HYPERDBG_HYPEREVADE)
 #    define IMPORT_EXPORT_HYPEREVADE __declspec(dllexport)
 #else
 #    define IMPORT_EXPORT_HYPEREVADE __declspec(dllimport)
diff --git a/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperLogImports.h b/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperLogImports.h
index 7324ed8d..582b7b73 100644
--- a/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperLogImports.h
+++ b/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperLogImports.h
@@ -10,7 +10,9 @@
  */
 #pragma once
 
-#ifdef HYPERDBG_HYPER_LOG
+#if defined(HYPERDBG_STATIC_BUILD)
+#    define IMPORT_EXPORT_HYPERLOG
+#elif defined(HYPERDBG_HYPER_LOG)
 #    define IMPORT_EXPORT_HYPERLOG __declspec(dllexport)
 #else
 #    define IMPORT_EXPORT_HYPERLOG __declspec(dllimport)
diff --git a/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperTrace.h b/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperTrace.h
index 2a190e7c..bda96585 100644
--- a/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperTrace.h
+++ b/hyperdbg/include/SDK/imports/kernel/HyperDbgHyperTrace.h
@@ -10,7 +10,9 @@
  */
 #pragma once
 
-#ifdef HYPERDBG_HYPERTRACE
+#if defined(HYPERDBG_STATIC_BUILD)
+#    define IMPORT_EXPORT_HYPERTRACE
+#elif defined(HYPERDBG_HYPERTRACE)
 #    define IMPORT_EXPORT_HYPERTRACE __declspec(dllexport)
 #else
 #    define IMPORT_EXPORT_HYPERTRACE __declspec(dllimport)
diff --git a/hyperdbg/include/SDK/imports/kernel/HyperDbgVmmImports.h b/hyperdbg/include/SDK/imports/kernel/HyperDbgVmmImports.h
index a738cb7c..47ebf689 100644
--- a/hyperdbg/include/SDK/imports/kernel/HyperDbgVmmImports.h
+++ b/hyperdbg/include/SDK/imports/kernel/HyperDbgVmmImports.h
@@ -10,7 +10,9 @@
  */
 #pragma once
 
-#ifdef HYPERDBG_VMM
+#if defined(HYPERDBG_STATIC_BUILD)
+#    define IMPORT_EXPORT_VMM
+#elif defined(HYPERDBG_VMM)
 #    define IMPORT_EXPORT_VMM __declspec(dllexport)
 #else
 #    define IMPORT_EXPORT_VMM __declspec(dllimport)
@@ -997,3 +999,38 @@ PciReadCam(WORD Bus, WORD Device, WORD Function, BYTE Offset, UINT8 Width);
 
 IMPORT_EXPORT_VMM BOOLEAN
 PciWriteCam(WORD Bus, WORD Device, WORD Function, BYTE Offset, UINT8 Width, QWORD Value);
+
+//////////////////////////////////////////////////
+//     Common Functions                       	//
+//////////////////////////////////////////////////
+
+IMPORT_EXPORT_VMM BOOLEAN
+CommonIsProcessExist(UINT32 ProcId);
+
+IMPORT_EXPORT_VMM BOOLEAN
+CommonValidateCoreNumber(UINT32 CoreNumber);
+
+IMPORT_EXPORT_VMM BOOLEAN
+CommonKillProcess(UINT32 ProcessId, UINT32 KillingMethod);
+
+//////////////////////////////////////////////////
+//     DPC Routines                           	//
+//////////////////////////////////////////////////
+
+IMPORT_EXPORT_VMM NTSTATUS
+DpcRoutineRunTaskOnSingleCore(UINT32 CoreNumber, PVOID Routine, PVOID DeferredContext);
+
+IMPORT_EXPORT_VMM VOID
+DpcRoutinePerformWriteMsr(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2);
+
+IMPORT_EXPORT_VMM VOID
+DpcRoutinePerformReadMsr(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2);
+
+IMPORT_EXPORT_VMM VOID
+DpcRoutineWriteMsrToAllCores(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2);
+
+IMPORT_EXPORT_VMM VOID
+DpcRoutineReadMsrToAllCores(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2);
+
+IMPORT_EXPORT_VMM BOOLEAN
+DpcRoutineSetHardwareDebugRegisters(KDPC * Dpc, PVOID DeferredContext, PVOID SystemArgument1, PVOID SystemArgument2);
diff --git a/hyperdbg/include/SDK/imports/user/HyperDbgScriptImports.h b/hyperdbg/include/SDK/imports/user/HyperDbgScriptImports.h
index 29f188fc..a3dd3a93 100644
--- a/hyperdbg/include/SDK/imports/user/HyperDbgScriptImports.h
+++ b/hyperdbg/include/SDK/imports/user/HyperDbgScriptImports.h
@@ -10,7 +10,9 @@
  */
 #pragma once
 
-#ifdef HYPERDBG_SCRIPT_ENGINE
+#if defined(HYPERDBG_STATIC_BUILD)
+#    define IMPORT_EXPORT_HYPERDBG_SCRIPT_ENGINE
+#elif defined(HYPERDBG_SCRIPT_ENGINE)
 #    define IMPORT_EXPORT_HYPERDBG_SCRIPT_ENGINE __declspec(dllexport)
 #else
 #    define IMPORT_EXPORT_HYPERDBG_SCRIPT_ENGINE __declspec(dllimport)
diff --git a/hyperdbg/include/SDK/imports/user/HyperDbgSymImports.h b/hyperdbg/include/SDK/imports/user/HyperDbgSymImports.h
index 98d97b53..6d71d909 100644
--- a/hyperdbg/include/SDK/imports/user/HyperDbgSymImports.h
+++ b/hyperdbg/include/SDK/imports/user/HyperDbgSymImports.h
@@ -10,7 +10,9 @@
  */
 #pragma once
 
-#ifdef HYPERDBG_SYMBOL_PARSER
+#if defined(HYPERDBG_STATIC_BUILD)
+#    define IMPORT_EXPORT_HYPERDBG_SYMBOL_PARSER
+#elif defined(HYPERDBG_SYMBOL_PARSER)
 #    define IMPORT_EXPORT_HYPERDBG_SYMBOL_PARSER __declspec(dllexport)
 #else
 #    define IMPORT_EXPORT_HYPERDBG_SYMBOL_PARSER __declspec(dllimport)
-- 
2.47.0.windows.2

